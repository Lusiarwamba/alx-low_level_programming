#include "main.h"

/**
 * _isupper - functions that checks uppercase characters
 * @c: char to be checked
 *
 * Return 0 or 1
 */
int _islower(int c)
{
	if (c = 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}

